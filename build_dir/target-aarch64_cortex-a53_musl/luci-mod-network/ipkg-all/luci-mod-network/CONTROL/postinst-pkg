[ -n "${IPKG_INSTROOT}" ] || {
	(. /etc/uci-defaults/50_luci-mod-admin-full) && rm -f /etc/uci-defaults/50_luci-mod-admin-full
	rm -f /tmp/luci-indexcache
	rm -rf /tmp/luci-modulecache/
	killall -HUP rpcd 2>/dev/null
	exit 0
}
